import { BerlinUhrTimePipe } from './berlin-uhr-time.pipe';

describe('BerlinUhrTimePipe', () => {
  it('create an instance', () => {
    const pipe = new BerlinUhrTimePipe();
    expect(pipe).toBeTruthy();
  });
});
